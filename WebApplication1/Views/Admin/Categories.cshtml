@model IEnumerable<WebApplication1.Models.Category>

@{
    ViewData["Title"] = "Manage Categories";
    Layout = "~/Views/Shared/admin-layout.cshtml";
}

<h2>Categories</h2>
<p>
    <a href="@Url.Action("Create", "Categories")" class="btn btn-primary">Create New Category</a>
</p>
<table class="table table-striped">
    <thead>
        <tr>
            <th>Name</th>
            <th>Description</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Name</td>
                <td>@item.Description</td>
                <td>
                    <a href="@Url.Action("Edit", "Categories", new { id = item.Id })" class="btn btn-sm btn-warning">Edit</a>
                    <a href="@Url.Action("Delete", "Categories", new { id = item.Id })" class="btn btn-sm btn-danger">Delete</a>
                    <a href="@Url.Action("Details", "Categories", new { id = item.Id })" class="btn btn-sm btn-info">Details</a>
                </td>
            </tr>
        }
    </tbody>
</table>
